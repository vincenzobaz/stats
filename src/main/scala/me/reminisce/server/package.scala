package me.reminisce

/**
  * Provides classes and actors which define the behavior of the application as an HTTP server, the entry point of the
  * application is [[me.reminisce.server.Server]]
  */
package object server